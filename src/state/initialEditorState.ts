import type { EditorDocument } from "../types/document";

export const INITIAL_EDITOR_DOCUMENT: EditorDocument = {
  version: 1,
  transform: {
    rotation: 0,
    flipX: false,
    flipY: false,
  },
};
